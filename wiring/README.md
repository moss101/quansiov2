# Canonical wiring registry

`service-wiring.json` is the machine-readable producer/consumer map. A feature may not introduce an alternate model, effect, machine, approval, task or recovery flow outside these canonical owners without an accepted architecture decision and registry change.
